@page
@model RazorPageSamples.Pages.QueryStringSample2Model
@{
}

@if (!string.IsNullOrEmpty(Model.Name))
{
    @Model.Name
}

@if (Model.Year != 0)
{
    @Model.Year
}

@if (Model.Month != 0)
{
    @Model.Month
}

